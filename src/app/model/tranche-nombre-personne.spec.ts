import { TrancheNombrePersonne } from './tranche-nombre-personne';

describe('TrancheNombrePersonne', () => {
  it('should create an instance', () => {
    expect(new TrancheNombrePersonne()).toBeTruthy();
  });
});
