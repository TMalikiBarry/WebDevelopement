import { InstrumentFinancement } from './instrument-financement';

describe('InstrumentFinancement', () => {
  it('should create an instance', () => {
    expect(new InstrumentFinancement()).toBeTruthy();
  });
});
