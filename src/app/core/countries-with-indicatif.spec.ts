import { CountriesWithIndicatif } from './countries-with-indicatif';

describe('CountriesWithIndicatif', () => {
  it('should create an instance', () => {
    expect(new CountriesWithIndicatif()).toBeTruthy();
  });
});
