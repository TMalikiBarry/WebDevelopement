import { PastDateValidator } from './past-date-validator';

describe('PastDateValidator', () => {
  it('should create an instance', () => {
    expect(new PastDateValidator()).toBeTruthy();
  });
});
